\documentclass[tikz]{standalone}
\usetikzlibrary{calc,trees,positioning,arrows,chains,shapes.geometric,%
    decorations.pathreplacing,decorations.pathmorphing,shapes,%
    matrix,shapes.symbols,fit}

\pgfdeclarelayer{back}
\pgfsetlayers{back,main}

\begin{document}
\begin{tikzpicture}
  \draw (0,0) grid (8,1);
  
  \foreach \i in {0,...,7}
  \node[font=\Large{}] at ($(\i,0)+(.5,.5)$) {\i}
  ;
   
  \foreach \i in {0}
  \draw[very thick,-latex] ($(\i,0)+(.5,1.75)$) -- ($(\i,0)+(.5,1.05)$) 
  ;

  \foreach \i in {1,...,7}
  {%
    %\draw[very thick,-latex,fill={gray!\i},draw={gray!\i}] ($(\pgfmathresult,0)+(.5,1.75)$) -- ($(\pgfmathresult,0)+(.5,1.05)$);
    \draw[very thick,-latex,fill={gray!70},draw={gray!70}] ($(\i,0)+(.5,1.75)$) -- ($(\i,0)+(.5,1.05)$);
  }

   
  ;

\end{tikzpicture}
\end{document}
